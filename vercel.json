{
  "builds": [
    {
      "src": "backend/config.ru",
      "use": "@vercel/ruby",
      "config": {
        "installCommand": "cd backend && bundle config set --local deployment true && bundle config set --local frozen true && bundle install",
        "buildCommand": "cd backend && bundle exec rails db:migrate"
      }
    }
  ],
  "routes": [
    {
      "src": "/(.*)",
      "dest": "backend/config.ru"
    }
  ]
}
