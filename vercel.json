{
  "builds": [
    {
      "src": "backend/config.ru",
      "use": "@vercel/ruby",
      "config": {
        "buildCommand": "cd backend && bundle exec rails db:migrate"
      }
    }
  ],
  "routes": [
    {
      "src": "/(.*)",
      "dest": "backend/config.ru"
    }
  ]
}
